
#include <stdio.h>
#include <unistd.h>
#include <sys/mman.h>
#include <sys/wait.h>
#include <stdbool.h>
#include <stdlib.h>


int fibonacci(int r=1,int n , int z,int w){
    do while (y=n.lenght)

    n[i+2]=n[i-1]+[i-2]
    }

int main(){
    
int numero_n=0;

int contador=1

printf("Ingresa la longitud de tu serie ");
scanf("%d",&numero_n);

int array[numero_n];
int n[numero_n];


size_t longitud=numero_n*sizeof(int);
//Para calcular la longitud de memoria de numero n y luego est aguardarla en una variable la cual sera la que nos permitira usarala en el mmap 
int *memoria = mmap(NULL, longitud, PROT_READ | PROT_WRITE, MAP_SHARED | MAP_ANONYMOUS, -1, 0);

pid_t hijo=fork();

int mitad_array=array[numero_n]/2;
memoria[0]=0;
memoria[1]=1;

if (hijo<0){
    exit(0);
    }
    
if(hijo>0){
    printf("Soy el padre ");
    for(int i=mitad_array;i<numero_n;i++){
        //ingresar metodo 
        wait(NULL);

        
        
    }
}
    
    }else if(hijo==0){
        memoria[0]=0;
        memoria[1]=1;
        printf("Soy el hijo");
        
        for(int i =2;i>mitad_array;i++){

            n[i]=memoria[i]-contador;
            contador ++;
        }
        for(int i=0;i<contad)
             z=n[i]+n[i+1];
            
  
   n[i+2]=n[i-1]+[i-2]
            
        
        
    }
    return 0;
    
        
    }
        

